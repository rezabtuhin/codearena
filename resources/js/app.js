import 'flowbite';
import './bootstrap';

// $(".tab-btn").each(function() {
//     // Get the current element
//     var $button = $(this);
    
//     // Get the classes you want to keep
//     var classesToKeep = "inline-block p-4 w-full tab-btn";
    
//     // Split the current classes by spaces
//     var currentClasses = $button.attr("class").split(" ");
    
//     // Filter out the classes you want to keep
//     var filteredClasses = currentClasses.filter(function(className) {
//         return classesToKeep.includes(className);
//     });
    
//     // Set the class attribute of the button with the filtered classes
//     $button.attr("class", filteredClasses.join(" "));
// });
